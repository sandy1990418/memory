# 中文技术笔记

## Redis 缓存配置

Redis 最大内存策略设置为 allkeys-lru。当内存达到上限时，系统会自动淘汰最近最少使用的键。
生产环境最大内存为 2GB，测试环境为 512MB。

## 数据库连接池

使用 PgBouncer 管理 PostgreSQL 连接池。连接池大小设置为 25。
支持事务级别的连接池模式，确保每个事务使用独立连接。

## 部署流程

采用蓝绿部署策略。新版本部署完成后，负载均衡器切换流量到新环境。
如果错误率超过 5%，系统自动回滚到上一个版本。
